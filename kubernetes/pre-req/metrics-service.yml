apiVersion: v1
kind: Service
metadata:
  name: metrics-server
  namespace: kube-system
spec:
  selector:
    app.kubernetes.io/name: metrics-server   # matches pod labels
  ports:
    - name: https
      port: 443
      targetPort: https
      protocol: TCP