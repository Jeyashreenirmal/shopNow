apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shopnow-ingress
  namespace: shopnow-jsree
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /jeyashree-admin
        pathType: Prefix
        backend:
          service:
            name: admin
            port:
              number: 80
      - path: /jeyashree
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80